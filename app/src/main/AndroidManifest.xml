<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ai2app.teampoker">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_team_poker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_team_poker"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Sample AdMob app ID: ca-app-pub-3940256099942544~3347511713 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/ad_unit_id"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".phone.PhoneMainActivity"
            android:label="@string/title_activity_phone_main"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".phone.UserChoiceActivity"
            android:label="@string/title_activity_user_choice"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".comm.PokerActivity"
            android:label="@string/title_activity_poker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ad.VideoAdActivity" />
        <activity android:name=".ad.InterstitialAdActivity" />
    </application>

</manifest>